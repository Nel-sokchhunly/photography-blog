const Loading = () => {
  return (
    <div
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
      className="h-full w-full"
    >
      loading...
    </div>
  );
};

export default Loading;
